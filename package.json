{
  "name": "serverless-cryptobot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "start:local": "npm run build && func host start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@types/node": "^14.11.8",
    "coingecko-api": "^1.0.10",
    "dotenv": "^8.2.0",
    "telegraf": "^3.38.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.2-beta2",
    "typescript": "^3.3.3"
  }
}
